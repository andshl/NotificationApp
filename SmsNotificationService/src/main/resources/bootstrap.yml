spring:
  application:
    name: sms-notification-service

server:
  port: 8001
  servlet:
    context-path: /sms-service

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://registry:8761/eureka/